# Static IP for eth0 (client-facing)
interface eth0
static ip_address=%%ETH0_IP%%/%%ETH0_CIDR%%
nolink

# Static IP for eth1 (USB dongle — PDU network)
interface eth1
static ip_address=%%ETH1_IP%%/%%ETH1_CIDR%%
nolink

# wlan0 gets DHCP (default behavior)
