# DHCP server on eth1 (USB dongle — PDU network)
interface=eth1
bind-dynamic

dhcp-range=%%DHCP_RANGE_START%%,%%DHCP_RANGE_END%%,%%ETH1_NETMASK%%,%%DHCP_LEASE_TIME%%

# Lease hook — writes PDU IP for nginx/ssh-bridge
dhcp-script=/usr/local/bin/pdu-lease-hook.sh

# Do not provide DNS
port=0
